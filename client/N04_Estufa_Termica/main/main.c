// Ponto de entrada da aplicação
// A função app_main() está definida em app/app_main.c
#include "app/app_main.h"

// No ESP-IDF, app_main() é chamado automaticamente pelo framework
// Este arquivo apenas garante que o header está incluído

